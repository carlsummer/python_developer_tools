# !/usr/bin/env python
# -- coding: utf-8 --
# @Author zengxiaohui
# Datatime:7/23/2021 10:52 AM
# @File:skimage_utils

"""
# 绘制直线， r0,c0 为起始点坐标，r1,c1为终止点坐标
# 返回rr,cc即 直线各个像素的位置
skimage.draw.line(r0, c0, r1, c1)

# 绘制抗锯齿的线
skimage.draw.line_aa(r0, c0, r1, c1)

# 绘制贝塞尔曲线
skimage.draw.bezier_curve(r0, c0, r1, c1, r2, c2, weight, shape=None)

# 绘制多边形
skimage.draw.polygon(r, c, shape=None)
"""

